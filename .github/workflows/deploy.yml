name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.0
        with:
          host: 149.102.157.23
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            /opt/deploy.sh
